# 통합 위키 이관 대기함

> 이 파일은 **임시 보관함**이다. 여기 있는 내용은 전부 `d:/Gcalen/wiki/`(통합 위키) 소관인데,
> 원격 자동화 세션은 저장소 밖 로컬 폴더를 읽을 수 없어서 일단 여기 적어둔다.
> 통합 위키로 옮긴 뒤에는 **이 파일에서 지운다.** 옮긴 항목은 `[이관 완료 YYYY-MM-DD]`로 표시.
>
> 경계 규칙([[SCHEMA]])대로 **프로젝트 이름 없이** 자체 완결형으로 썼으니 그대로 붙여 넣으면 된다.
> 각 항목 제목 옆의 `→ 파일명`이 목적지다.
>
> 최초 작성 2026-07-29 (SEO 전면 점검 + 데이터 큐레이션 작업 중 확보)

---

## 대기 중인 항목

(없음 — 새로 확보한 재사용 기술 지식이 있으면 여기에 목적지 파일과 함께 적을 것)

---

## 이관 이력

### [이관 완료 2026-07-30] 2026-07-28~29 SEO·데이터 큐레이션 19건
로컬 세션에서 통합 위키로 전량 반영. 목적지별 결과:

- **`seo.md`** — 목록 모달로 인한 고아 URL(색인 누락 주원인)과 링크+preventDefault 해법,
  저품질 페이지 noindex·사이트맵 제외 동기화, `lastmod`에 빌드 시각 금지, `x-default` 지정,
  구조화 데이터 "아는 만큼만"(startDate UTC 오프셋·endDate·organizer.url 금지·경고vs오류),
  도메인 이전 301+주소 변경 도구, 신규 도메인은 시간이 필요.
  ↳ 이관 중 **기존 서술 2건을 정정**: `organizer.url 권장` → 자기 URL 넣지 말 것,
  `단일 공연은 endDate 생략` → 하루짜리는 `startDate == endDate`가 구글 권장(시각까지 아는
  경우만 생략). 실전 검증 결과가 기존 위키 내용보다 정확했다.
- **`nextjs.md`** — 클라 컴포넌트 `usePathname()`은 SSG에서도 정상, 데이터 JSON 라운드트립
  선검증, 직접 만든 frontmatter 파서의 CRLF 정규화.
- **`vercel.md`** — 도메인 리다이렉트는 대시보드 전용(CLI 불가)·배포 없는 도메인 404,
  Node 런타임 버전은 리포가 아닌 대시보드 설정이라는 점 추가.
- **`web-scraping.md`(신설)** — 검색 요약 날짜 조작, 재유통 기사, 이미지 육안 검증(치수로
  못 거름), 승인 샘플 대조, Wikimedia 썸네일 규칙·429, 차단되는 소스 대안.
- **`windows-dev.md`** — `perl -CSD`의 비ASCII 리터럴 함정, PowerShell 5.1 `2>&1` 종료코드
  오염, 동적 정규식 교차 검증.
