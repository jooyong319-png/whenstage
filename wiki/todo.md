# 남은 작업 (2026-07-29 갱신)

## 완료된 것 (참고용 — 다시 하지 말 것)
- [x] 카피 정리("게임" 표현 → 콘서트 도메인) — about/guide/contact/privacy/terms/blog/news,
      i18nLabels.ts의 EN/JA "game" 잔재(홈 meta description 포함, SEO 버그였음)까지 전수 수정
- [x] 콘서트 전용 리서처 프롬프트(RESEARCHER_KO/EN/JA), 뉴스/블로그/아티스트 리서처 프롬프트
      전부 로케일별 완전 독립 구조로 정착, `prompts/` 폴더로 정리
- [x] 아티스트 목록/상세, 공연장 목록/상세, 상세 페이지 사이드바 맥락화
- [x] 콘텐츠 자동화(`prompts/PLANNER.md`) + 기획/개발 자동화(`PRODUCT_PLANNER`/`PRODUCT_DEVELOPER`)
      파이프라인 구축, `BACKLOG.md`로 인계
- [x] Vercel 배포 연결 확인, Supabase 프로젝트를 이 프로젝트 전용으로 완전히 새로 구성
      (gcalen과 분리 완료 — 예전엔 "미확인"이었으나 2026-07-23에 실제 env 설정하며 확인됨)
- [x] 1차+2차 비주얼 오버홀(Motion 도입 → "무대 조명 + 티켓 스텁" 컨셉으로 전면 재작업),
      gcalen 죽은 코드/CSS/컴포넌트 대청소([[decisions]]/[[design-audit]] 참고)
- [x] 에드센스 승인용 콘텐츠 볼륨 확보(블로그/뉴스/아티스트 소개글 대량 보강)
- [x] SEO 1차 감사·수정 — `<html lang>` 서버 렌더링(멀티 루트 레이아웃), OG 태그, JSON-LD
      4종(Event/MusicGroup/MusicVenue/Article/NewsArticle), hreflang, Google Search Console
      등록 + sitemap 제출까지 완료
      ⚠️ **"SEO는 끝났다"는 뜻이 아니다.** 위 항목은 태그·마크업 층만 본 것이고, 2026-07-28에
      더 큰 문제들이 따로 나왔다: 목록이 전부 모달이라 상세 페이지로 가는 링크가 서버 HTML에
      없어 **ko 콘서트 34개 중 11개가 고아 URL**이었고, 공연 1건짜리 저품질 페이지가 99개,
      sitemap `lastmod`가 전부 빌드 시각이었다. 전부 해결됨 — 경위·판단 근거는 [[decisions]]
      2026-07-28 항목 4개 참고. 앞으로도 "마크업이 맞다 = 색인된다"로 넘기지 말 것.
- [x] 웹푸시 알림 파이프라인 KO 전용 → KO/EN/JA 전체 대응, "껐는데 새로고침하면 다시 켜짐"
      버그 수정(permission과 subscription은 다른 상태라는 게 원인이었음)
- [x] 검색에 아티스트 영문/로마자 별칭 매칭(`data/artist-aliases.json`) 추가
- [x] 사용자 제보 기능(예매 링크 제보/수정, 아티스트 정보 수정·등록 신청) + 관리자 검토 큐

## 사용자가 직접 처리해야 하는 것(자동화 프롬프트가 손댈 수 없는 영역)
- [ ] **OG 이미지 캐시 갱신** — `public/og-image.png`는 이미 신규(다크 네이비)인데 카카오/SNS가
      옛 분홍 그라데이션을 캐시 중. 카카오 캐시 초기화 도구 or 공유 URL에 `?v=` 파라미터로 강제.
- [ ] **알림 발송 시각용 외부 크론 유지** — cron-job.org 매시간 잡 + Vercel env `CRON_HOURLY=1` +
      Supabase `alter table push_subscriptions add column prefs jsonb`(적용 완료). 이 세 개가
      살아있어야 유저별 발송 시각이 동작(Hobby 플랜 제약). 자세한 건 [[architecture]].
- [ ] **서치 콘솔 주소 변경 실행: gcalen.com → whenstage.com** (2026-07-29 등록)
      이전 서비스 gcalen.com(게임 캘린더)은 두 달간 약 450클릭을 쌓았고 도메인도 아직 보유 중
      (만료 2027-05-27). 서비스 종료 후 Vercel 배포가 없어 **전 경로 404로 방치**돼 있던 것을
      2026-07-29에 `gcalen.com`·`www.gcalen.com` → `whenstage.com` **301 리다이렉트**로 연결했다
      (Vercel Domains에서 설정, 외부 curl로 301 확인 완료).
      남은 절차:
      1. gcalen을 등록해 둔 **다른 구글 계정**에서 서치 콘솔에 `whenstage.com` 속성 추가 →
         HTML 태그 방식으로 확인. 태그는 이미 배포돼 있음(layout.tsx의 verification.google 두 번째 값)
      2. 같은 계정에서 `gcalen.com` 속성 → 설정 → **주소 변경** → `whenstage.com` 지정
      - ⚠️ 인증 태그 2개는 주소 변경 후에도 **지우지 말 것** — 빼면 그 계정이 속성 접근을 잃는다
      - ⚠️ **도메인 갱신** — 만료 2027-05-27. 등록기관에서 **자동 갱신을 켜둘 것**(비용보다
        깜빡하고 만료시키는 게 실질 위험이다. 유예기간이 지나면 남이 가져갈 수 있고 되돌릴 수 없다).
        갱신 기간 판단: **2027-05 필수**(구글이 "리다이렉트 최소 1년 유지" 권고), **2028-05 권장**
        (그때도 whenstage가 2년차라 자체 신호가 얇다), 그 이후는 "gcalen.com을 다시 쓸 생각이
        있는가"만 기준으로. 만료시켜도 이미 넘어온 신호가 되돌아가진 않지만, 남은 외부 링크가
        갈 곳을 잃고 제3자가 등록해 갈 수 있다.
      - ✅ **gcalen.com 재사용 계획 없음**(2026-07-29 사용자 확정). 되돌릴 여지를 남길 필요가
        없으므로 **주소 변경 도구까지 그대로 진행**한다. (참고: 301과 도메인 나이는 언제든
        되돌릴 수 있지만 주소 변경 도구는 취소 절차가 구글 문서에 없어 확인 불가 — 재사용
        가능성이 있었다면 주소 변경만 보류하는 선택지가 있었다는 뜻이고, 지금은 해당 없음)
      - 갱신은 2028-05까지만 하고 그 뒤엔 놓아줘도 된다. 그때쯤이면 whenstage 자체 신호가
        gcalen에서 오는 몫보다 훨씬 커진다.
      - 기대치: 게임 검색어 트래픽(450클릭)이 옮겨오는 게 아니라, **백링크와 도메인 이력**이 넘어와
        콘서트 검색어에서 유리해지는 것이 목적. 콘텐츠 주제가 완전히 달라 100% 이전은 기대하기 어렵다

- [ ] **콘텐츠 자동화 스케줄을 하루 18회 → 7회로 축소** (2026-07-29 등록)
      리서처 3종·뉴스 3종이 각각 **하루 2회**(리서처 09/21시, 뉴스 10/22시)로 잡혀 있어 하루 18회가
      돈다. 2026-07-25에 토큰이 소진돼 **7/26~7/29 자동화가 완전히 멈췄다**(그 나흘치는 수동으로 메움).
      실측상 하루 산출물은 뉴스 3건 수준이라 2회는 과하다 — 두 번째 실행은 48시간 창이 이미 소진돼
      대체로 빈손이 된다. 권장:
      - 리서처 KO/EN/JA **09:00 1회**, 뉴스 KO/EN/JA **10:00 1회** (21/22시 삭제)
      - 블로그는 KO→EN→JA 로테이션으로 **2~3일에 1편**(현재 22편인데 공연 데이터가 로케일당 36~40건
        이라 하루 1편이면 곧 주제가 재탕된다)
      - ⚠️ **리서처가 뉴스보다 먼저** 도는 순서는 반드시 유지. 뉴스 기사가 방금 등재된 공연에
        `/[lang]/concert/<id>` 링크를 걸어야 히어로 이미지와 내부 링크가 살아난다.
      - 프롬프트 파일 자체는 손대지 말 것. 뉴스·리서처를 로케일별 한 프롬프트로 **통합하는 안은
        검토 후 기각**했다 — 통합의 명분이던 "기사가 공연에 링크를 못 건다"는 문제는 리서처가
        이미 1시간 먼저 돌기 때문에 실제로는 없고(2026-07-28에 그 문제가 관측된 건 수동 실행에서
        순서를 거꾸로 돌린 탓), 남는 이득인 조사 중복 절감 3~4할보다 300줄 프롬프트 3개를 새로
        쓰고 유지하는 비용이 크다.

## 다듬을거리(있으면 좋음, 급하지 않음)
- [ ] en/ja 홈 타이틀에도 "일정/캘린더" 정체 단어 정렬(ko는 완료) — 선택.
- [ ] 찜 추가/제거 토스트("찜 목록에 추가됨")가 한국어 고정 — EN/JA 로케일화(첫-찜 넛지는 이미
      로케일 반영됨). 기존 잔재 i18n 갭.

## 기획/개발 자동화(`prompts/PRODUCT_PLANNER.md`/`prompts/PRODUCT_DEVELOPER.md`)가 판단해서 진행할 것
`BACKLOG.md`가 실제 소스 오브 트루스(상태 최신). 2026-07-23 기준 대기 중인 항목:
- [ ] `-01` 저장소 루트 잔재 `og-image.png`(gcalen 스캐폴드, `public/og-image.png`와는 별개) 제거
- [ ] `-03` `components/PreRegCountdown.module.css` → `TicketingPhase.module.css` 리네임
      (컴포넌트는 이미 TicketingPhase인데 CSS 파일명만 gcalen 시절 이름 그대로 남음)
- [x] `-04` (P1) soft-404 **해결(2026-07-24)** — 상세 5종(concert/news/blog/artist/venue)에
      `export const dynamicParams = false;` → 빌드에 없는 slug는 진짜 404. `curl`로 404 확인.
      (원인·일반 해법은 통합 위키 [nextjs.md] "soft-404" 참고)

그 외엔 이 문서 + `PROJECT_STATUS.md` + `BACKLOG.md` + 실제 코드베이스를 보고 스스로 다음
우선순위를 판단할 것(막연한 신규 아이디어보다 관찰된 구체적 문제 우선). 최대 3개까지만
새로 등록하고, 확신 가는 게 없으면 0개도 정상.
