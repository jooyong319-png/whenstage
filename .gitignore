node_modules/
.next/
.env.local
.env*.local
tsconfig.tsbuildinfo
*.tsbuildinfo
.DS_Store

# 옵시디언 볼트 워크스페이스(위키를 옵시디언으로 열 때 생김) — 문서(.md)는 커밋하되
# 워크스페이스 상태/플러그인 캐시는 잦은 변경이라 커밋 안 함
.obsidian/

# 빌드가 자동 생성 — 커밋해두면 rebase 때마다 충돌한다
next-env.d.ts
.claude/
# Vercel CLI 연결 정보 (vercel link가 생성)
.vercel
# 위쪽 .env.local / .env*.local 에 더해, 확장자 없는 .env 도 막는다.
# .env.example 은 "뭐가 필요한지" 알려주는 문서라 예외 (2026-09-08에 세 저장소에 추가했다)
.env*
!.env.example
